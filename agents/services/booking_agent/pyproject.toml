[project]
name = "booking-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["medapp-agents"]

[tool.uv.sources]
medapp-agents = { path = "../..", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
