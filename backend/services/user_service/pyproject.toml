[project]
name = "user-service"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "medapp-shared",
  "argon2-cffi>=23.1",
  "email-validator>=2.2",
]

[tool.uv.sources]
medapp-shared = { path = "../../shared", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
