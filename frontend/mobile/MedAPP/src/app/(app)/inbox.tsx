// Inbox route — thin wrapper.
// Reached via the BottomNav "Inbox" tab from any root screen.

import { InboxScreen } from "@/features/chat/InboxScreen";

export default function InboxRoute() {
  return <InboxScreen />;
}
