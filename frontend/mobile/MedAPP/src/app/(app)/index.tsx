// Authenticated home route — thin wrapper, screen component lives in feature.

import { HomeScreen } from "@/features/home/HomeScreen";

export default function HomeRoute() {
  return <HomeScreen />;
}
