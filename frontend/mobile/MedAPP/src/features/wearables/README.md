# features/wearables/

Device pairing, sync status, and on-device summary views for wearable data.

Talks to the `wearable_sync_service` backend. The heavy data lives server-side; this feature renders summaries and exposes a "Sync now" affordance.
