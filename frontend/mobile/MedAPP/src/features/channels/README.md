# features/channels/

Health channels — topical content streams users can follow.

- **All users:** browse, subscribe, read posts.
- **Partners only:** create channels, publish to them, manage subscribers.

Authoring UI is gated by `<RequirePartner>` from `components/gating/`.
