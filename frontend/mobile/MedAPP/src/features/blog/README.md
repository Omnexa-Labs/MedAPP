# features/blog/

Blog posts — long-form articles by partners.

- **All users:** read, like, comment, share.
- **Partners only:** author, edit, publish, view analytics.

Authoring UI is gated by `<RequirePartner>` from `components/gating/`.
