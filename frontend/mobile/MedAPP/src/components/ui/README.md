# components/ui/

Presentation primitives — `Button`, `Card`, `Input`, `Avatar`, `Badge`, `RoleBadge`. No business logic, no data fetching.

Grows as the design system materialises. For now, screens use raw `Pressable`/`Text`/`View` with NativeWind classes; primitives get extracted here once a pattern repeats in two or more screens.
