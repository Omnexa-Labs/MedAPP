# components/feedback/

User-visible feedback primitives: `Toast`, `ErrorBoundary`, inline error banners, success confirmations, loading shimmers.

Anything that communicates state-of-the-world to the user lives here so the look stays consistent across features.
