# components/layout/

Page-level structural wrappers: `Screen`, `KeyboardAvoidingScreen`, `ScrollScreen`, `EmptyState`, `ListSkeleton`, max-width containers.

These set padding, safe-area handling, and default backgrounds so individual screen files don't repeat themselves.
