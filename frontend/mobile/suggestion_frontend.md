*Suggested follow-ups (not implemented) *
  # 1. Swap the DD/MM/YYYY DOB inputs for @react-native-community/datetimepicker.
  # 2. Add brand-coloured Google/Apple icons (or # # expo-auth-session-driven flows) in Step 1.
  # 3. After sign-up success, if enableBiometric is true, # trigger the biometric enrollment
  # prompt before landing on (app).
