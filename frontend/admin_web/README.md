# MedApp Admin Console (Next.js)

Internal-only admin UI for KYC verification, dispute handling, content moderation, and analytics dashboards.

Not part of the patient-facing product. Authenticated via SSO (Google Workspace) and gated by `ADMIN` role.

```bash
pnpm install
pnpm dev
```
