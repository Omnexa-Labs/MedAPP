apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking_service
  labels: { app: booking_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: booking_service }
  template:
    metadata:
      labels: { app: booking_service }
    spec:
      serviceAccountName: booking_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8005 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8005 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8005 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: booking_service
spec:
  selector: { app: booking_service }
  ports:
    - port: 8005
      targetPort: 8005
