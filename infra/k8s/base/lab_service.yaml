apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab_service
  labels: { app: lab_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: lab_service }
  template:
    metadata:
      labels: { app: lab_service }
    spec:
      serviceAccountName: lab_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8009 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8009 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8009 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: lab_service
spec:
  selector: { app: lab_service }
  ports:
    - port: 8009
      targetPort: 8009
