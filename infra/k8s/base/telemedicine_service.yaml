apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemedicine_service
  labels: { app: telemedicine_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: telemedicine_service }
  template:
    metadata:
      labels: { app: telemedicine_service }
    spec:
      serviceAccountName: telemedicine_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8007 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8007 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8007 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: telemedicine_service
spec:
  selector: { app: telemedicine_service }
  ports:
    - port: 8007
      targetPort: 8007
