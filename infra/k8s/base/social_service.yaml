apiVersion: apps/v1
kind: Deployment
metadata:
  name: social_service
  labels: { app: social_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: social_service }
  template:
    metadata:
      labels: { app: social_service }
    spec:
      serviceAccountName: social_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8011 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8011 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8011 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: social_service
spec:
  selector: { app: social_service }
  ports:
    - port: 8011
      targetPort: 8011
