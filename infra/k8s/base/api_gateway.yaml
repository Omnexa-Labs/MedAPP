apiVersion: apps/v1
kind: Deployment
metadata:
  name: api_gateway
  labels: { app: api_gateway }
spec:
  replicas: 2
  selector:
    matchLabels: { app: api_gateway }
  template:
    metadata:
      labels: { app: api_gateway }
    spec:
      serviceAccountName: api_gateway
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: api_gateway
spec:
  selector: { app: api_gateway }
  ports:
    - port: 8000
      targetPort: 8000
