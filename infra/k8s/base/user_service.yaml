apiVersion: apps/v1
kind: Deployment
metadata:
  name: user_service
  labels: { app: user_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: user_service }
  template:
    metadata:
      labels: { app: user_service }
    spec:
      serviceAccountName: user_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8001 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8001 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8001 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: user_service
spec:
  selector: { app: user_service }
  ports:
    - port: 8001
      targetPort: 8001
