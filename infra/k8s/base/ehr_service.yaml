apiVersion: apps/v1
kind: Deployment
metadata:
  name: ehr_service
  labels: { app: ehr_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: ehr_service }
  template:
    metadata:
      labels: { app: ehr_service }
    spec:
      serviceAccountName: ehr_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8010 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8010 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8010 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: ehr_service
spec:
  selector: { app: ehr_service }
  ports:
    - port: 8010
      targetPort: 8010
