apiVersion: apps/v1
kind: Deployment
metadata:
  name: nurse_service
  labels: { app: nurse_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: nurse_service }
  template:
    metadata:
      labels: { app: nurse_service }
    spec:
      serviceAccountName: nurse_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8003 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8003 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8003 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: nurse_service
spec:
  selector: { app: nurse_service }
  ports:
    - port: 8003
      targetPort: 8003
