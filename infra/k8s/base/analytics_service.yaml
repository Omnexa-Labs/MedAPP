apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics_service
  labels: { app: analytics_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: analytics_service }
  template:
    metadata:
      labels: { app: analytics_service }
    spec:
      serviceAccountName: analytics_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8012 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8012 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8012 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: analytics_service
spec:
  selector: { app: analytics_service }
  ports:
    - port: 8012
      targetPort: 8012
