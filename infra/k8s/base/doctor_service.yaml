apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor_service
  labels: { app: doctor_service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: doctor_service }
  template:
    metadata:
      labels: { app: doctor_service }
    spec:
      serviceAccountName: doctor_service
      containers:
        - name: app
          image: REPLACE_IMAGE_REF
          ports: [{ containerPort: 8002 }]
          envFrom:
            - configMapRef: { name: medapp-config }
            - secretRef: { name: medapp-secrets }
          readinessProbe:
            httpGet: { path: /healthz, port: 8002 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: 8002 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1, memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: doctor_service
spec:
  selector: { app: doctor_service }
  ports:
    - port: 8002
      targetPort: 8002
